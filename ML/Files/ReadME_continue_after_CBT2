Continue with changing data.ipynb. Go to last tab and try to extract the link to actual resumes. Use the "indeed resume scraper master" as the help. Extract the links to the resumes and then use already written code to get the resume part from the page.
